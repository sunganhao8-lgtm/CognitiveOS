__pycache__/
.env
.venv/
node_modules/
.idea/
.vscode/
*.log

# CognitiveOS local data — never commit user knowledge
knowledge/
dashboard/
.cogos/
index.html

# Persona training artefacts — local, never committed
user/persona/samples/
user/persona/drivel.jsonl
user/persona/model.md

# Private conversation history — never commit
user/conversations/

# Generated rule files (only seeded on first verify run)
user/rules/*.json

# Verify results — local audit trail
user/verify/
# Generated per-QA detail .md files — derived from private conversations
user/conversations/details/

# Local symlinks to external skill libraries (e.g. emilkowalski/skills)
.skills-emil/
